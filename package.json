{
  "name": "cypress-tests",
  "version": "1.0.0",
  "description": "Cypress tests for Google homepage",
  "scripts": {
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}