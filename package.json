{
  "name": "cypress-tests",
  "version": "1.0.0",
  "description": "Basic Cypress tests",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run": "cypress run --headless --browser chrome"
  },
  "devDependencies": {
    "cypress": "^9.5.1"
  }
}
